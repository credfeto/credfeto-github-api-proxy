services:
  proxy:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PROXY_TOKEN=${PROXY_TOKEN}
      - GITHUB_PAT=${GITHUB_PAT}
      - PORT=3000
    restart: unless-stopped
